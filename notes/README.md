# notes

This folder is the assistant's whole world. It runs here with read and write access to these
files and nothing else, so everything you dictate lands in this directory as plain Markdown.

It starts empty on purpose.
